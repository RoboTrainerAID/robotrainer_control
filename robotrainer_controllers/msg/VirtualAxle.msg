# x and y specify a point on the virtual axle (in platform coordinates). 
# a specifies the normal of the axis, i.e. the direction the axle would roll
# towards, if the wheels move with equal velocity.
float64 x
float64 y
float64 a
